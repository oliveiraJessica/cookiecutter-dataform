{
    "warehouse": "{{cookiecutter.warehouse}}",
    "defaultSchema": "{{cookiecutter.defaultSchema}}",
    "assertionSchema": "{{cookiecutter.assertionSchema}}",
    "defaultDatabase": "{{cookiecutter.defaultDatabase}}"
}
